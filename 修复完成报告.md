# CompanyRag 项目修复完成报告

## 🆕 最新修复 (2026-07-25): Gitee 流水线 Maven 构建失败

### 问题 1: SpringDoc 依赖版本缺失
**错误信息**:
```
[ERROR] 'dependencies.dependency.version' for org.springdoc:springdoc-openapi-starter-webmvc-ui:jar is missing
```

**根因分析**:
- `company-rag-web/pom.xml` 中引用了 `springdoc-openapi-starter-webmvc-ui` 依赖
- 根目录 `pom.xml` 中缺少 `springdoc.version` 属性定义
- `<dependencyManagement>` 中缺少该依赖的版本管理

**修复方案**:
1. 在根目录 `pom.xml` 的 `<properties>` 中添加:
   ```xml
   <springdoc.version>2.8.6</springdoc.version>
   ```
2. 在 `<dependencyManagement>` 中添加依赖定义:
   ```xml
   <dependency>
       <groupId>org.springdoc</groupId>
       <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
       <version>${springdoc.version}</version>
   </dependency>
   ```
3. 恢复被误删的 `<profiles>` 配置（集成测试 Profile）

**修改文件**:
- `pom.xml` - 根目录

---

### 问题 2: IntegrationTest 注解编译失败
**错误信息**:
```
[ERROR] /root/workspace/.../IntegrationTest.java:[3,29] package org.junit.jupiter.api does not exist
[ERROR] /root/workspace/.../IntegrationTest.java:[22,2] cannot find symbol - SpringBootTest
[ERROR] /root/workspace/.../IntegrationTest.java:[24,2] cannot find symbol - Tag
```

**根因分析**:
- `IntegrationTest.java` 自定义注解使用了 `@Tag`、`@SpringBootTest`、`@ActiveProfiles` 等注解
- `company-rag-common/pom.xml` 中 `spring-boot-starter-test` 依赖的 scope 为 `test`
- 主代码（main/java）无法使用 test scope 的依赖，只有测试代码（test/java）可以使用

**修复方案**:
修改 `company-rag-common/pom.xml`，移除 `spring-boot-starter-test` 的 `<scope>test</scope>`，使其在 compile scope 下可用：
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-test</artifactId>
    <!-- 移除 <scope>test</scope>，因为 IntegrationTest 注解在主代码中使用 -->
</dependency>
```

**修改文件**:
- `company-rag-common/pom.xml`

---

### 问题 3: 流水线测试排除配置
**背景**: 流水线环境没有 Redis 和 PostgreSQL，需要跳过集成测试

**原有问题**: 
- 使用 `-Dtest='!@IntegrationTest'` 语法无法排除标记了 `@IntegrationTest` 注解的测试
- Maven Surefire 的 `-Dtest` 参数基于类名匹配，不支持注解排除

**修复方案**:
1. 在 `IntegrationTest.java` 注解上添加 `@Tag("integration-test")`:
   ```java
   @Tag("integration-test")
   public @interface IntegrationTest {
   }
   ```
2. 修改 Gitee 流水线 commands:
   ```yaml
   commands:
   - mvn clean package -B -DexcludedGroups=integration-test -Dsurefire.failIfNoSpecifiedTests=false
   ```

**修改文件**:
- `company-rag-common/src/main/java/com/company/rag/common/IntegrationTest.java`
- `.gitee/workflows/ci.yml` (流水线配置)

---

## ✅ 历史修复问题

### 1. Spring Cloud Function 兼容性问题
- **问题**: Spring Cloud Function 4.1.1 与 Spring Boot 3.4.4 不兼容
- **修复**: 
  - 在 `CompanyRagApplication.java` 中排除 `ContextFunctionCatalogAutoConfiguration`
  - 在 `application.yml` 中禁用 `spring.cloud.function.enabled=false`

### 2. Spring AI 版本冲突
- **问题**: Spring AI DashScope 1.1.0 与 Spring AI Core 1.0.4 API不兼容
- **修复**:
  - 将 `spring-ai.version` 从 1.1.0 改为 1.0.4
  - 删除父POM中单独定义的 `spring-ai-dashscope-spring-boot-starter`
  - 修正 groupId: `group.springframework.ai` → `org.springframework.ai`
  - 所有模块统一使用BOM管理版本

### 3. SQL注入安全漏洞
- **修复**: DatabaseQueryTool 添加多层安全检查

### 4. 多租户架构
- **修复**: 实现PostgreSQL RLS行级安全策略

### 5. Rerank实现
- **修复**: 使用LLM评分替代简单TF-IDF

---

## 📋 验证步骤

### 步骤1: 刷新Maven依赖

**IntelliJ IDEA**:
1. 右键项目根目录
2. Maven → Reload Project
3. 等待依赖下载完成

**Eclipse**:
1. 右键项目
2. Maven → Update Project
3. 勾选 "Force Update of Snapshots/Releases"
4. 点击OK

---

### 步骤2: 清理编译

在项目根目录执行:

```bash
mvn clean compile -DskipTests
```

**预期结果**: BUILD SUCCESS

如果编译失败,请检查:
- JDK版本是否为17
- Maven版本是否为3.6+
- 网络是否正常(需要下载依赖)

---

### 步骤3: 启动项目

#### 方式1: IDE启动
- 找到 `CompanyRagApplication.java`
- 右键 → Run 'CompanyRagApplication'

#### 方式2: 命令行启动
```bash
mvn spring-boot:run -pl company-rag-bootstrap
```

#### 方式3: 打包后启动
```bash
mvn clean package -DskipTests
java -jar company-rag-bootstrap\target\company-rag-bootstrap-1.0.0-SNAPSHOT.jar
```

---

### 步骤4: 验证启动成功

**成功标志**:
```
Started CompanyRagApplication in X.XXX seconds
```

**访问测试**:
- 主页: http://localhost:8080
- 健康检查: http://localhost:8080/actuator/health
- Prometheus指标: http://localhost:8080/actuator/prometheus

---

## ⚠️ 可能遇到的问题

### 问题1: 数据库连接失败

**错误信息**: 
```
Connection refused: localhost:5433
```

**解决方案**:
1. 启动PostgreSQL数据库
2. 检查端口是否为5433(默认5432)
3. 检查数据库 `company_rag` 是否存在
4. 执行 `sql/init.sql` 初始化脚本

**快速启动PostgreSQL**:
```bash
docker run -d \
  --name company-rag-postgres \
  -e POSTGRES_DB=company_rag \
  -e POSTGRES_USER=postgres \
  -e POSTGRES_PASSWORD=difyai123456 \
  -p 5433:5432 \
  pgvector/pgvector:pg16
```

---

### 问题2: Redis连接失败

**错误信息**:
```
Unable to connect to Redis
```

**解决方案**:
1. 启动Redis服务
2. 检查端口6379是否开放

**快速启动Redis**:
```bash
docker run -d \
  --name company-rag-redis \
  -p 6379:6379 \
  redis:7-alpine
```

---

### 问题3: API Key未配置

**错误信息**:
```
DashScope API key is not configured
```

**解决方案**:
设置环境变量:
```bash
# Windows
set DASHSCOPE_API_KEY=your-api-key

# Linux/Mac
export DASHSCOPE_API_KEY=your-api-key
```

或在 `application.yml` 中直接配置:
```yaml
spring:
  ai:
    dashscope:
      api-key: your-api-key
```

---

### 问题4: 端口占用

**错误信息**:
```
Port 8080 is already in use
```

**解决方案**:
修改 `application.yml`:
```yaml
server:
  port: 8081  # 改为其他端口
```

---

## 📊 依赖版本确认

修复后的关键依赖版本:

| 依赖 | 版本 | 说明 |
|------|------|------|
| Spring Boot | 3.4.4 | ✅ |
| Spring AI BOM | 1.0.4 | ✅ 统一管理 |
| Spring AI DashScope | 1.0.4 | ✅ BOM管理 |
| Spring AI PgVector | 1.0.4 | ✅ BOM管理 |
| MyBatis-Plus | 3.5.9 | ✅ |
| Resilience4j | 2.2.0 | ✅ |
| Redisson | 3.40.2 | ✅ |
| Apache Tika | 3.1.0 | ✅ |

---

## 🎯 下一步建议

项目启动成功后,建议:

1. **执行数据库初始化**
   ```sql
   -- 连接数据库
   psql -h localhost -p 5433 -U postgres -d company_rag
   
   -- 执行初始化脚本
   \i sql/init.sql
   ```

2. **测试核心功能**
   - 上传文档: POST /api/document/upload
   - RAG检索: POST /api/rag/search
   - 流式回答: POST /api/rag/stream

3. **补充单元测试**
   - 目前测试覆盖率为0
   - 建议至少达到70%

4. **前端集成**
   - 当前使用CDN方式
   - 建议打包Vue3静态资源嵌入

---

## 📞 问题反馈

如果按照以上步骤仍有问题,请提供:

1. 完整的错误堆栈信息
2. Maven编译输出
3. JDK版本: `java -version`
4. Maven版本: `mvn -version`

我会继续协助解决。
