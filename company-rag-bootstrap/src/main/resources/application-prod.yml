spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/company_rag?currentSchema=public
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}
  data:
    redis:
      host: redis
      port: 6379

logging:
  level:
    com.company.rag: INFO
