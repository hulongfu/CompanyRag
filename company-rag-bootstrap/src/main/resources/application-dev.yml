spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/company_rag?currentSchema=public
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY:sk-your-dev-key}
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.company.rag: DEBUG
